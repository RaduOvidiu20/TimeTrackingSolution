@model TaskType;

@{
    Layout = "_Layout";
    ViewBag.Title = "Create Task";

}
<form asp-controller="TaskType" method="post" class="container w-75">
    <div class="card mt-5">
        <div class="card-header">
            <h2 class="card-title">@ViewBag.Title</h2>
            <a asp-controller="TaskType" asp-action="GetAll" class="btn btn-info mt-3 mb-3">Back to Employees list</a>
        </div>
        <div class="card-body">
            <div class="form-group" hidden="hidden">
                <label asp-for="TaskTypeId" class="form-label">Task ID</label>
                <input asp-for="TaskTypeId" class="form-control"/>
            </div>
            <div class="form-group">
                <label asp-for="Name" class="form-label">Task Name</label>
                <input asp-for="Name" class="form-control"/>
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
        </div>
        <div class="card-footer">
            <button type="submit" class="btn btn-primary" asp-action="Create">@ViewBag.Title</button>
        </div>
    </div>
</form>