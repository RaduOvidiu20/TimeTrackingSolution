@model TaskType;

@{
    Layout = "_Layout";
    ViewBag.Title = "Edit Task";

}
<form asp-controller="TaskType" method="post" class="container w-75">
    <div class="card mt-5">
        <div class="card-header">
            <h2 class="card-title">@ViewBag.Title</h2>
            <a asp-controller="TaskType" asp-action="GetAll" class="btn btn-success mt-3 mb-3">Back to projects list</a>
        </div>
        <div class="card-body">
            <div class="form-group">
                <label asp-for="TaskTypeId" class="form-label">Task ID</label>
                <input asp-for="TaskTypeId" class="form-control" readonly="readonly"/>
            </div>
            <div class="form-group">
                <label asp-for="Name" class="form-label">Task Name</label>
                <input asp-for="Name" class="form-control"/>
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
        </div>
        <div class="card-footer">
            <button type="submit" class="btn btn-warning" asp-action="Edit">@ViewBag.Title</button>
        </div>
    </div>
</form>