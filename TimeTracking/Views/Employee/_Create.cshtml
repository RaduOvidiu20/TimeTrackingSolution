@model Employee;

@{
    Layout = "_Layout";
    ViewBag.Title = "Create Employee";

}
<form asp-controller="Employee" method="post">
    <div class="card mt-5">
        <div class="card-header">
            <h2 class="card-title">@ViewBag.Title</h2>
            <a asp-controller="Employee" asp-action="GetAll" class="btn btn-info mt-3 mb-3">Back to Employees list</a>
        </div>
        <div class="card-body">
            <div class="form-group" hidden="hidden">
                <label asp-for="EmployeeId" class="form-label">Employee ID</label>
                <input asp-for="EmployeeId" class="form-control"/>
            </div>
            <div class="form-group">
                <label asp-for="Name" class="form-label">Employee Name</label>
                <input asp-for="Name" class="form-control"/>
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Age" class="form-label">Employee Age</label>
                <input asp-for="Age" type="number" class="form-control"/>
                <span asp-validation-for="Age" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Phone" class="form-label">Employee Phone Number</label>
                <input asp-for="Phone" type="number" class="form-control"/>
                <span asp-validation-for="Phone" class="text-danger"></span>
            </div>
        </div>
        <div class="card-footer">
            <button type="submit" class="btn btn-primary" asp-action="Create">@ViewBag.Title</button>
        </div>
    </div>
</form>